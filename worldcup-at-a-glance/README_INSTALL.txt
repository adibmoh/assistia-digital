WorldCup PWA v29 final mobile fix

Fixes:
- Desktop remains the clean version.
- iPhone standings changed to compact rows, not big cards.
- Adds visible Install app button near Refresh.
- Service worker cache version bumped to v29.

Replace all files in:
assistia-digital/worldcup-at-a-glance/

After deployment:
- Desktop: Ctrl + F5.
- iPhone/Safari: clear site data if old PWA cache remains, then reload.
